# Optional: run UI without Tauri against HTTP sidecar
# terminal 1: WIKIHOME_MOCK=1 pnpm dev:http
# terminal 2: set VITE_SIDECAR_HTTP and pnpm --filter @wikihome/desktop dev
VITE_SIDECAR_HTTP=http://127.0.0.1:8787
